--- 
apiVersion: apps/v1
kind: Deployment
metadata: 
  annotations: 
    deployment.kubernetes.io/revision: "1"
  creationTimestamp: "2021-02-07T12:50:11Z"
  generation: 1
  labels: 
    app: demo-app
  managedFields: 
    - 
      apiVersion: apps/v1
      fieldsType: FieldsV1
      fieldsV1: 
        ? "f:metadata"
        : 
          ? "f:labels"
          : 
            ? "."
            : {}
            ? "f:app"
            : {}
        ? "f:spec"
        : 
          ? "f:progressDeadlineSeconds"
          : {}
          ? "f:replicas"
          : {}
          ? "f:revisionHistoryLimit"
          : {}
          ? "f:selector"
          : 
            ? "f:matchLabels"
            : 
              ? "."
              : {}
              ? "f:app"
              : {}
          ? "f:strategy"
          : 
            ? "f:rollingUpdate"
            : 
              ? "."
              : {}
              ? "f:maxSurge"
              : {}
              ? "f:maxUnavailable"
              : {}
            ? "f:type"
            : {}
          ? "f:template"
          : 
            ? "f:metadata"
            : 
              ? "f:labels"
              : 
                ? "."
                : {}
                ? "f:app"
                : {}
            ? "f:spec"
            : 
              ? "f:containers"
              : 
                ? "k:{\"name\":\"k8s-demo-spring-mongo\"}"
                : 
                  ? "."
                  : {}
                  ? "f:image"
                  : {}
                  ? "f:imagePullPolicy"
                  : {}
                  ? "f:name"
                  : {}
                  ? "f:resources"
                  : {}
                  ? "f:terminationMessagePath"
                  : {}
                  ? "f:terminationMessagePolicy"
                  : {}
              ? "f:dnsPolicy"
              : {}
              ? "f:restartPolicy"
              : {}
              ? "f:schedulerName"
              : {}
              ? "f:securityContext"
              : {}
              ? "f:terminationGracePeriodSeconds"
              : {}
      manager: kubectl
      operation: Update
      time: "2021-02-07T12:50:11Z"
    - 
      apiVersion: apps/v1
      fieldsType: FieldsV1
      fieldsV1: 
        ? "f:metadata"
        : 
          ? "f:annotations"
          : 
            ? "."
            : {}
            ? "f:deployment.kubernetes.io/revision"
            : {}
        ? "f:status"
        : 
          ? "f:availableReplicas"
          : {}
          ? "f:conditions"
          : 
            ? "."
            : {}
            ? "k:{\"type\":\"Available\"}"
            : 
              ? "."
              : {}
              ? "f:lastTransitionTime"
              : {}
              ? "f:lastUpdateTime"
              : {}
              ? "f:message"
              : {}
              ? "f:reason"
              : {}
              ? "f:status"
              : {}
              ? "f:type"
              : {}
            ? "k:{\"type\":\"Progressing\"}"
            : 
              ? "."
              : {}
              ? "f:lastTransitionTime"
              : {}
              ? "f:lastUpdateTime"
              : {}
              ? "f:message"
              : {}
              ? "f:reason"
              : {}
              ? "f:status"
              : {}
              ? "f:type"
              : {}
          ? "f:observedGeneration"
          : {}
          ? "f:readyReplicas"
          : {}
          ? "f:replicas"
          : {}
          ? "f:updatedReplicas"
          : {}
      manager: kube-controller-manager
      operation: Update
      time: "2021-02-07T12:50:19Z"
  name: demo-app
  namespace: demo
  resourceVersion: "492826"
  selfLink: /apis/apps/v1/namespaces/demo/deployments/demo-app
  uid: b60c868a-4ca5-4660-8229-145edef4138f
spec: 
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector: 
    matchLabels: 
      app: demo-app
  strategy: 
    rollingUpdate: 
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template: 
    metadata: 
      creationTimestamp: ~
      labels: 
        app: demo-app
    spec: 
      containers: 
        - env: 
            - name: SPRING_DATA_MONGODB_HOST
              value: mongo
          image: "k8s-demo-spring-mongo:1"
          imagePullPolicy: IfNotPresent
          name: k8s-demo-spring-mongo
          resources: {}
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
status: 
  availableReplicas: 1
  conditions: 
    - 
      lastTransitionTime: "2021-02-07T12:50:19Z"
      lastUpdateTime: "2021-02-07T12:50:19Z"
      message: "Deployment has minimum availability."
      reason: MinimumReplicasAvailable
      status: "True"
      type: Available
    - 
      lastTransitionTime: "2021-02-07T12:50:11Z"
      lastUpdateTime: "2021-02-07T12:50:19Z"
      message: "ReplicaSet \"demo-app-54d6b7f948\" has successfully progressed."
      reason: NewReplicaSetAvailable
      status: "True"
      type: Progressing
  observedGeneration: 1
  readyReplicas: 1
  replicas: 1
  updatedReplicas: 1
